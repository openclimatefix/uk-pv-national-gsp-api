name: Lint Python
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout source repo
      uses: actions/checkout@v2
    - name: Linting with Pylinter
      uses: alexanderdamiani/pylinter@v1.2.0
